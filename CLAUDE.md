# CLAUDE.md

Read `AGENTS.md` first. It is this skeleton's functional and operational source of truth.

Also read `../../AGENTS.md` for workspace rules and cross-repo boundaries.

Use `manifest.json`, stack docs, skeleton source files, Docker Compose config, and local scripts as the implementation source of truth.

Do not duplicate skeleton rules here. Update `AGENTS.md` when the contract changes.
